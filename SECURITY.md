# Security

Never commit passwords, personal access tokens, OAuth tokens, cookies, private keys, Zotero library identifiers, private vault paths, or unpublished datasets.

Use GitHub browser authentication, an operating-system credential manager, or a scoped token supplied through an environment variable. If a credential is committed, revoke or rotate it first, then remove it from the complete Git history before publishing.
