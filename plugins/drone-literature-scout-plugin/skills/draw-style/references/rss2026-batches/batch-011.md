# RSS 2026 优秀图语料：第 011 批（RSS2026_051–RSS2026_055）

> 状态：待用户确认，未冻结；以下结论尚未并入冻结语料。本批只对用户明确重点称赞的 RSS2026_052 记录精确色号，其他图仅分析颜色职责。

## RSS2026_051
<!-- rss2026-meta {"visible_figure_label":"Fig. 8","figure_track":"data/table","domain_tags":["Ablation Study","Online Adaptation","Tracking Error"]} -->

### 1. 截图身份

- 原文件：屏幕截图 2026-08-02 005508.png。
- 定位信息：`Fig. 8: Ablation study of residual dynamics and anchored rollouts.`。
- 图的范式类别：2×2 多指标消融折线图。
- **用户个人学习重点**：整体一般但尚可；确认四图为 2＋2 配置。淡灰粗虚线、橙色 `ε_th = 0.35` 下方区域、浅色带和白色外圈节点让数据图不单调；网格略密，虚线段可更长。配色较合理但不要求本批精确色号。
- **补充核读**：四图按列绑定 `Lap time / Error`，按行绑定 `Line / Fig8` 两个任务或场景，横轴统一为 `Policy Version`。右列同时给阈值线和阈值以下的浅桃安全域；浅带看起来像不确定性，但 caption 没有定义 SD、SE、CI 或范围。

### 2. 图本身表达什么

五种消融配置从 Policy Version 0 演化到 12：左列圈速整体下降，右列 tracking error 在约 0.16–0.32 m 范围内波动。橙色虚线标出 `ε_th = 0.35`，caption 说明所有方法在降低 lap time 的同时维持低于阈值的有界误差；`Ours` 在圈速上最终最好。

### 3. 逐元素与连接核读

**2×2 面板：**

- 左上 `Line: Lap time`、右上 `Line: Error`；左下 `Fig8: Lap time`、右下 `Fig8: Error`。
- 同列共享量纲，同排共享任务；左列 y 轴在左，右列 y 轴及标签置于右，减少中缝拥挤。
- 两个下方面板显示 `Policy Version`，上排省略重复 x 标签。

**曲线与节点：**

- Baseline、LOTF、Anchor Only、Res. Only、Ours 各用固定色线和圆节点。
- 节点内为方法色，外圈为白色细描边；这使节点在同色浅带、网格和交叉线中仍可定位。
- 多条曲线有同色半透明 ribbon；可见宽度随版本变化，但其统计定义未写在图例或 caption 中。

**阈值与背景：**

- 右列橙色横虚线锚定 `ε_th = 0.35`，标签置于左上空白区。
- 阈值以下填充大面积浅桃色，表达“可接受域”，并让数据较少的右图不显空。
- 阈值上方不填色，形成语义明确的上下区域，而不是装饰性渐变。

**网格与图例：**

- 横纵网格均为粗浅灰短虚线；当前段长较短、密度偏高，视觉上形成方格幕布。
- 顶部五项图例置于浅灰大圆角容器，分两行排列；容器与图主体分离。

### 4. 全局构图与阅读路径

阅读先按行比较同一场景下的效率与约束，再向下比较另一个场景。2×2 让“收益—代价”在水平位置上固定；右列背景域是视觉锚，左列曲线下降是趋势锚。图例占据完整顶部，但没有侵入数据区。

### 5. 局部视觉语法

- 圆节点白色描边是低成本的局部对比增强，尤其适合节点落在同色带或网格上。
- 语义区域填充比无意义背景色更有效：浅桃只代表阈值以下可行域。
- 网格宜改为更细、更稀、单段更长的淡灰虚线；保留主刻度网格，删除次要方向或部分纵网格。
- 本图颜色只记录“方法色＋同色带＋橙色约束域”的职责，不录精确 HEX。

### 6. 为什么部分有效

有效之处是信息量足：四个面板、五条方法线、阈值域和白边节点共同避免“只有两条线”的单调；收益与约束同时展示也形成完整论证。一般之处是灰网格过密、图例和字体偏大、所有折线视觉权重接近，导致数据层次不够清爽。

### 7. 可以吸收的绘图知识

- 2×2 适合用“行＝场景、列＝效率/约束”组织消融，前提是两列语义固定。
- 阈值区域可用低饱和填充提供语境，不能仅为了填满画布添加无含义底色。
- 端点/采样点加 1–2 px 白色外圈，可在浅带、网格和线交叉处提高可见性。
- 同色 ribbon 只有在统计口径已定义后才绘制；否则只能称“浅色带”。
- 网格虚线宜采用长 dash、细线、低 alpha，减少棋盘感。

### 8. 不足、误读风险与不应照搬

浅带统计口径未定义；标题 `Line/Fig8` 脱离正文不完全自明；图例过宽；灰网格短而密；五条线交叉时仍难追踪；右列安全域可能让读者误以为背景深浅编码风险大小。不能把浅带擅称标准差。

### 9. 本图最值得学习什么（收录理由）

学习“2×2 收益—约束消融＋语义阈值域＋白边节点”的组合，尤其是用有含义的背景区域和节点描边防止数据图过空；不学习过密短虚线网格。

### 10. 证据边界

浅带是 SD、SE、CI、范围还是跨种子包络未确认；Line/Fig8 的正式任务定义、每点样本量、随机种子数、误差聚合方式和曲线插值口径未从截图完整确认。

## RSS2026_052
<!-- rss2026-meta {"visible_figure_label":"Fig. 2","figure_track":"mixed","domain_tags":["Fisher Information","Experimental Design","3D Surface","Trajectory"]} -->

### 1. 截图身份

- 原文件：屏幕截图 2026-08-02 005655.png。
- 定位信息：`Fig. 2: Information gain trajectories.`；论文为 *Learning What Matters: Adaptive Information-Theoretic Objectives for Robot Exploration*。
- 图的范式类别：3D 信息地形数据图＋2D 局部动力学解释图。
- **用户个人学习重点**：形式新颖、重点参考。上方山形 3D 网格、黑色连续点轨迹与蓝/绿/红阶段点很高级；下方红蓝绿箭头、白色外轮廓和柔和弯曲值得学习，还要知道如何用 MATLAB/Python 生成。
- **补充核读**：上排是 mass `θ1` 与 friction `θ2` 上的 Fisher information landscape，不是任意山形装饰；三列对应 Vanilla BOED Eq. (5)、Agnostic QOED Eq. (9)、QOED (ours) Eq. (12)。下排不是上图的俯视投影，而是动力学局部几何解释。

### 2. 图本身表达什么

Vanilla BOED 可能让轨迹离开 identifiable ridge；Agnostic QOED 更靠近 ridge，但仍可能激发 non-identifiable 方向；QOED 通过约束/正则压制这些方向，使轨迹沿高信息 ridge 演化。上排给全局信息地形与轨迹，下排解释每一步的 observable、non-identifiable 与 method-step 方向。

### 3. 逐元素与连接核读

**上排 3D：**

- 每列用浅蓝半透明 surface＋细蓝 wireframe 展示双参数地形；高度轴为 Information Gain。
- 黑色连续线和密集小黑点沿曲面前进，保留优化轨迹的采样顺序。
- 蓝色圆点标识起始/早期状态，绿色标识方法步骤或中间状态，红色标识后期/终点；彩色点有黑或白色边缘以抵抗曲面干扰。
- 三列相机角度、轴向和曲面视觉语法一致，便于只比较轨迹几何。

**下排 2D：**

- 黑色曲线表示 dynamics trajectory，外侧浅灰软带提供局部上下文；其概率/不确定性语义未被 caption 单独定义。
- 红箭头标 `observable`，蓝箭头标 `non-identifiable`，绿色箭头对应 `method step`；箭头由共同起点向不同方向发散。
- 箭头外有白色粗 halo，文字直接使用同色，并避开黑曲线；白边把三色箭头从灰带、黑线和彼此交叠中分离。
- 淡绿竖虚线分隔局部阶段；三列仍保持相同语法，但箭头方向和轨迹贴合程度不同。

**跨排关系：**

- 上排回答轨迹在 Fisher information landscape 上走到哪里；下排回答某一步为何沿可观测或不可辨识方向移动。
- 列标题与方程号把两排锁定为同一种方法，不需要跨排长箭头。

### 4. 全局构图与阅读路径

先从左到右比较三种目标，再从每列上方向下解释局部几何。上排高信息密度、下排留白较多；`Information Gain` 与 `Dynamics` 两个纵向粗标题形成行索引。三列由细灰竖线分隔，避免六幅小图散开。

### 5. 局部视觉语法

- 原生代表色：浅蓝地形 `#D6E9F2`，蓝线/箭头 `#1F76B3`，红箭头 `#D62628`，绿箭头 `#1A974F`；较深 wireframe 可取 `#5698C6`。
- 三个高饱和方向色只占箭头与关键节点，小面积使用；大面积地形保持浅蓝，背景保持白。
- 黑色轨迹承担连续性，彩色大点承担阶段，形状/轮廓与颜色形成冗余编码。
- 白色 halo 不是阴影，而是前景分离层；特别适合箭头穿过曲线或浅带时使用。

### 6. 为什么有效

它把抽象的“可辨识方向”变成两个互补空间：3D 地形显示全局目标几何，2D 局部箭头显示方向分解。浅蓝大面、黑色轨迹和少量红蓝绿形成清晰面积层级；白色外轮廓防止箭头变脏，曲线和箭头都采用圆滑路径，因此技术图仍显柔和。

### 7. 可以吸收的绘图知识

- **Python/Matplotlib 复现骨架**：用 `meshgrid` 生成 `θ1/θ2`，`Axes3D.plot_surface(..., alpha≈0.30–0.45, edgecolor='#5698C6', linewidth≈0.3)` 或 `plot_wireframe` 绘地形；用 `ax.plot3D` 画黑色轨迹并用小圆点显示采样，再用 `scatter` 叠蓝/绿/红阶段点。固定 `view_init`、轴域和 box aspect，三列禁止各自自动取景。
- 下排可用 `fill_between` 画浅灰软带、`FancyArrowPatch` 画柔和弯箭头，并用 `matplotlib.patheffects.withStroke(foreground='white')` 或 `path_effects` 添加白色外轮廓。
- 3D surface 的高度、轨迹和点必须来自同一坐标；不要把表面纹理当纯背景。
- 上排全局、下排局部的组合适合势能、信息增益、可观测性、损失地形和约束方向解释。

### 8. 不足、误读风险与不应照搬

3D z 轴刻度几乎省略，精确读值能力弱；下排浅灰带语义不清；蓝/绿阶段点的正式时序需依赖正文；透视可能夸大山峰高度。不能从曲面平滑度推断采样密度，也不能把下排当作上排严格投影。

### 9. 本图最值得学习什么（收录理由）

重点收录“浅蓝 3D 信息地形＋黑色采样轨迹＋阶段点”和“白边柔和方向箭头”的上下证据闭环，并给出可直接用于 Python 代码生成的实现语法。

### 10. 证据边界

作者实际使用 MATLAB、Python 还是后期矢量软件未从论文源明确确认；上述代码是视觉等价复现方案，不冒充原始源码。下排灰带、阶段点时序、表面插值和网格采样密度未完全确认。

## RSS2026_053
<!-- rss2026-meta {"visible_figure_label":"Fig. 2","figure_track":"method/schematic","domain_tags":["Statistical Manifold","Local Geometry","Tangent Plane","Covariance"]} -->

### 1. 截图身份

- 原文件：屏幕截图 2026-08-02 005756.png。
- 定位信息：`Fig. 2: A visualization illustrating how underlying geometry can be represented as a statistical manifold.`。
- 图的范式类别：3D 网格曲面＋局部切平面统计流形示意图。
- **用户个人学习重点**：弯曲网格形象，弯曲处截取平面并相互对应的表达值得参考；小符号位置合理，配色可参考但不要求精确字号/色号。
- **补充核读**：图不是只画一个曲面，而是从兔形网格对象的局部邻域，抽象出点 `p/q` 的局部流形、切平面/高斯邻域与共同底层几何 `M_g`。

### 2. 图本身表达什么

Caption 说明：在不同坐标帧中观察到的两个点，可视为来自同一底层分布的样本。左侧对象上的局部几何被展开为 `M_p`、`M_q`，两者通过坐标变换 `T_p/T_q` 对应到共同流形 `M_g`；蓝色平面和红色椭圆表达局部统计分布 `N(x̄,C)`。

### 3. 逐元素与连接核读

**对象与局部邻域：**

- 左上是兔形黑色 wireframe mesh，表面放置三个蓝绿渐变球/邻域，说明从真实对象截取局部几何。
- `Local Geometry` 黑色弧箭头从对象指向右上共同流形，承担从实例到抽象的转换。

**三个流形视图：**

- `M_p`、`M_q`、`M_g` 都用浅绿弯曲网格表示，保持同一种曲率语言。
- 蓝色半透明多边形切过每个弯曲网格，表示局部切平面/统计邻域支撑面；红色椭圆贴在曲面附近，表达协方差主方向。
- 点 `p` 为红色、`q` 为蓝色；黑色虚线把它们映射到上方共同视图中的对应点。

**坐标帧与符号：**

- `T_p`、`T_q` 位于上方两角，各配红绿蓝三轴小箭头；位置远离流形密集区，避免与公式争夺。
- `N(x̄_p,C_p)`、`N(x̄_q,C_q)` 和共同分布记号直接贴近蓝色平面下沿，既锚定对象又不遮挡点。
- `T_p p`、`T_q q` 放在对应点旁；红色变换弧在共同流形上连接两点。

### 4. 全局构图与阅读路径

阅读由左上真实对象进入，沿 `Local Geometry` 到右上共同抽象，再由两条虚线回溯左下 `p` 与右下 `q`。三个曲面形成三角构图；共同流形位于顶点，两个局部流形位于底边，天然表达“两个局部→一个共享几何”。

### 5. 局部视觉语法

- 绿网格固定表示流形，蓝平面固定表示局部统计平面，红椭圆固定表示协方差方向，黑虚线固定表示跨表示对应。
- 符号沿对象边缘或平面下沿放置，不放在网格最密处；点用实色＋黑边增强锚定。
- 曲面、平面、椭圆和点按“面→局部面→统计轮廓→样本”的尺度递减，层级明确。

### 6. 为什么有效

它没有用长段文字解释 statistical manifold，而是让同一局部几何在实例、局部坐标和共享坐标中重复出现。切平面在曲率最大的附近截取，使“局部可近似平面、全局仍弯曲”一眼可见；符号都贴近对应几何对象，减少图例查找。

### 7. 可以吸收的绘图知识

- 曲面问题可用“全局弯曲网格＋局部切平面＋协方差椭圆＋样本点”表达局部线性化。
- 代码生成时用参数化曲面/`plot_surface` 或 `plot_wireframe` 画流形，用四角点构造半透明 `Poly3DCollection` 切平面，用椭圆的特征向量/特征值生成红色统计轮廓；导出后再用矢量软件排列兔形 mesh 和公式。
- 同一对象跨坐标系时，点颜色、符号名和连接虚线必须成套复用。
- 适用于流形学习、SE(3) 局部坐标、协方差传播、切空间和几何深度学习。

### 8. 不足、误读风险与不应照搬

兔形网格、球形邻域、蓝平面和红椭圆的精确数学关系需正文支持；透视下切平面可能看似与曲面相交但不代表真实截面；公式较多且部分重叠网格。不能把示意椭圆称为 95% CI，也不能把蓝平面理解为物理切割板。

### 9. 本图最值得学习什么（收录理由）

学习三角式“两个局部视图→一个共同流形”和弯曲处插入局部平面/统计椭圆的表达，尤其是符号沿几何边缘放置而不遮挡核心曲率。

### 10. 证据边界

兔形 mesh 的数据来源、球体是否代表邻域核、蓝平面的正式定义、红椭圆概率质量、变换方向和所有协方差构造未从截图独立确认。

## RSS2026_054
<!-- rss2026-meta {"visible_figure_label":"Fig. 4","figure_track":"method/schematic","domain_tags":["LiDAR","Covariance Estimator","PointNet++","Network Architecture"]} -->

### 1. 截图身份

- 原文件：屏幕截图 2026-08-02 005819.png。
- 定位信息：`Fig. 4: Network architecture of covariance estimator.`。
- 图的范式类别：点云网络流程架构图。
- **用户个人学习重点**：配色很差、流程一般，作为反面教材。
- **补充核读**：语义链条仍可辨认：LiDAR Scan→Sampling & Ball Query→Latent Encoding→Interpolation→Point-wise Covariance，并有底部长 skip connection；问题主要在视觉编码和空间组织。

### 2. 图本身表达什么

输入 LiDAR 点经过采样与球查询，邻域点分别经 MLP 编码为 ball latent；插值阶段把 latent 回传到点级，再经 MLP 输出 Cholesky 风格下三角参数，最终形成每点协方差椭圆。上下两行代表多个点的重复处理，中间省略号表示批量延伸。

### 3. 逐元素与连接核读

- 绿色圆点表示采样/查询中心，蓝色半透明大圆表示 ball neighborhood，虚点表示邻域点。
- 三个 MLP 矩形并行接收中心与邻域特征，汇入蓝绿渐变 `Ball latent`。
- 插值阶段 latent 一对多连回局部点，再经三只 MLP 输出六个下三角参数。
- 黑箭头连接主流程；蓝色点线表示跨阶段点对应；底部粗黑折线从原始扫描跨越大半画布返回插值阶段，标 `Skip Connection`。
- 最右蓝青椭圆＋绿点示意 point-wise covariance。

### 4. 全局构图与阅读路径

五个巨大标题横排，主体以左右线性流程展开；上下重复两条实例，中间用竖向省略号连接。底部长 skip connection 占据大面积空白并与主阅读方向竞争。

### 5. 局部视觉语法

- 高饱和蓝圆、亮绿点、蓝绿渐变框、黑粗边和黑粗箭头同时竞争，没有主次。
- 同一蓝绿渐变既用于 latent 模块又用于最终协方差，角色不够专一。
- 标题、模块字和图注比例差异大；所有 MLP 黑边过粗，造成拼贴感。

### 6. 为什么一般

流程能读是因为严格左到右且重复结构清楚；视觉差是因为色相职责混乱、每个对象都有粗黑边、模块没有层级容器、skip connection 过长、标题过大。大量文字和几何都以最高权重出现，无法形成视觉入口。

### 7. 可以吸收的绘图知识

- 反例规则：输入点、邻域、神经模块、latent 和输出不应全部使用相同饱和度与粗描边。
- 重复 MLP 可先放入一个轻容器并标 `shared MLP ×3`，减少三个独立重框。
- Skip connection 应沿容器边缘走短路径，并使用较细、不同线型的箭头，不占据整幅图底部。
- 渐变只用于真实连续量或空间对象，不用来让普通矩形“更像神经网络”。

### 8. 不足、误读风险与不应照搬

颜色不高级；粗边、粗箭头和大标题叠加；重复结构冗长；点线/实线语义无图例；下三角参数到协方差的变换未解释；skip connection 起终点虽可追踪但路线过长。不要照搬蓝绿渐变矩形和所有对象黑粗边。

### 9. 本图最值得学习什么（收录理由）

作为 M3 反例：即使数据流正确，若没有颜色职责、容器层级和箭头权重，流程图仍会显得杂乱且廉价。

### 10. 证据边界

MLP 是否共享权重、ball query 半径、插值方法、skip 特征、下三角参数的正定约束、椭圆维度和上下两行具体样本身份未从截图确认。

## RSS2026_055
<!-- rss2026-meta {"visible_figure_label":"Fig. 5","figure_track":"data/table","domain_tags":["Runtime Composition","Pie Chart","Profiling"]} -->

### 1. 截图身份

- 原文件：屏幕截图 2026-08-02 010012.png。
- 定位信息：`Fig. 5: Runtime composition of POLI training pipeline.`。
- 图的范式类别：带外置引线标注的运行时组成饼图。
- **用户个人学习重点**：经典饼图；优点是用细折线把小部分引到外侧，写出名称、毫秒和比例，块之间有白色分隔。
- **补充核读**：四个组成分别为 Covariance Estimation 46.4 ms (62.8%)、Neural Network Update 17.6 ms (23.9%)、Certifiably Correct Differentiable Optimization 9.4 ms (12.7%)、Loss Calculation 0.5 ms (0.7%)。

### 2. 图本身表达什么

POLI 训练一次主要时间花在 covariance estimation，其次是 neural network update 和可认证可微优化；loss calculation 只占 0.7%。饼图展示组成占比，标签同时保留绝对耗时和百分比。

### 3. 逐元素与连接核读

- 四块以白色细分隔线切开；最大深蓝扇区超过半圆，左侧标签单独放置。
- 三个右侧标签按扇区垂直位置分层，细 leader line 先从扇区边缘斜出，再折为短水平线。
- 0.7% 的极薄绿色扇区几乎无法直接容纳文字，外置折线让其仍可被识别。
- 标签每项包含模块名、毫秒和括号百分比；没有重复图例。

### 4. 全局构图与阅读路径

饼图居中略偏左，最大扇区标签放左侧，其他三项放右侧，形成左右重量平衡。右侧长标签按从上到下的扇区顺序排列，leader 不交叉。

### 5. 局部视觉语法

- 白色 wedge separator 是类别边界，不是阴影。
- 细灰黑折线比粗箭头克制；折点靠近饼图，水平段靠近文字，建立清楚锚定。
- 不需要在扇区内部再写百分比，避免重复和狭窄区拥挤。

### 6. 为什么有效

它解决了饼图最常见的小扇区标注问题：所有标签外置并有确定引线，不要求把 0.7% 挤进扇区。绝对时间和百分比并列也让图既能看组成又能看真实开销。

### 7. 可以吸收的绘图知识

- Matplotlib 可用 `pie(..., wedgeprops={'edgecolor':'white','linewidth':1})`；依据每个 wedge 中心角计算锚点，再用 `annotate(..., arrowprops={'arrowstyle':'-','connectionstyle':'angle3'})` 或两段折线外置标签。
- 小扇区统一外标，标签按 y 排序并做最小间距避让；引线禁止交叉。
- 绝对值＋百分比比只有百分比更适合 runtime/profile 图。
- 类别超过 5–6 个或需要精确比较时，改用排序横向条形图。

### 8. 不足、误读风险与不应照搬

饼图对 23.9% 与 12.7% 的精确比较仍弱于条形图；长模块名造成大面积文字；配色普通，绿色 0.7% 太薄；没有总运行时说明。白色分隔线会略微夸大小扇区边界，线宽应很轻。

### 9. 本图最值得学习什么（收录理由）

学习“白色扇区分隔＋细折线外置完整标签”，尤其用于极小比例仍必须显示名称、绝对值和百分比的场景。

### 10. 证据边界

四项耗时的统计单位、硬件、batch size、测量次数、均值/中位数、总时长、是否并行和 rounding 导致的 100.1% 合计误差未从截图确认。

## 本批综合

### 视觉家族

- RSS2026_051：2×2 收益—约束消融、语义阈值域、白边节点和未定义浅带。
- RSS2026_052：3D 信息地形＋轨迹与 2D 局部方向解释。
- RSS2026_053：曲面、局部切平面、统计椭圆和跨坐标对应。
- RSS2026_054：点云网络流程反例。
- RSS2026_055：带外置折线标签的 runtime 饼图。

### 合并后的精华

- 051、052、053 共同说明：数据图不单调不等于堆装饰，而是增加有语义的约束域、局部几何、轨迹、阶段点或跨表示解释。
- 052、053 归入 M7＋M8：连续地形负责全局，轨迹/切平面/方向箭头负责局部可解释性。
- 054 作为 M3 反例：粗边、同饱和渐变、超长 skip 和等权标题会摧毁流程层级。
- 055 补充 M6：小比例组成必须通过引线或改用条形图保持可读。
- 本批只有 052 因用户明确重点称赞配色而进入精确色板候选。

以上均保持待确认，不自动冻结。
